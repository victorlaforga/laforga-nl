import React from "react";
import { MainSection } from "./section-5/index";
// import '../styles/styles.scss'
const SectionFive = ({ lang }) => {
  return (
    <section className="section-five">
      <MainSection lang={lang} />
    </section>
  );
};

export default SectionFive;
